jobs:
  build:
    working_directory: ~/circleci-demo-java-spring
    steps:
      - run: 
        command: 'echo "test yml"'
test:
  override:
    - java -version 
    - echo CIRCLE_BRANCH

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - build
      - test1:
          requires:
            - build
      - test2:
          requires:
            - test1
      - deploy:
          requires:
            - test2
